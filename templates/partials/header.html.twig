<nav class="navbar navbar-expand-sm navbar-dark bg-primary mb-3">
    <div class="container">
        <a href="/" class="navbar-brand">API Maraîchage</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#mobile-nav">
            <span class="navbar-toggle-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="mobile-nav">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a class="nav-link {% if current_menu is defined and current_menu == 'home' %}active{% endif %}"
                       href="{{ path('home') }}" class="nav-link">Home</a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link {% if current_menu is defined and current_menu == 'mesures' %}active{% endif %}"
                       href="#" class="nav-link dropdown-toggle" data-toggle="dropdown">Mesures</a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                        <a href="{{ path('mesures_tab') }}" class="dropdown-item">Tableaux de données</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ path('mesures_index') }}" class="dropdown-item">Courbes de tendance</a>
{#                        <div class="dropdown-divider"></div>#}
{#                        <a href="{{ path('mesures_par_date') }}" class="dropdown-item">par date</a>#}
                    </div>
                </li>
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a href="{{ path('admin_user_index') }}" class="nav-link">Administration</a>
                    </li>
                {% endif %}
                {% if not app.user %}
                    <li class="nav-item">
                        <a href="{{ path('security_login') }}" class="nav-link">Connexion</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a href="{{ path('security_logout') }}" class="nav-link">Déconnexion</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>